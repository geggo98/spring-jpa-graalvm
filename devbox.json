{
  "packages": [
    "wrk@latest",
    "curl@latest",
    "time@latest",
    "procps@latest",
    "path:./graalvm-closed-source-flake/"
  ],
  "shell": {
    "init_hook": [
      "export JAVA_HOME=$(dirname $(dirname $(which java)))",
      "export GRAALVM_HOME=$(dirname $(dirname $(which native-image)))"
    ],
    "scripts": {
      "test": [
        "./gradlew clean test nativeTest"
      ]
    }
  }
}
